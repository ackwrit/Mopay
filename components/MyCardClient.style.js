import { StyleSheet } from "react-native";

export const styleCardClient = StyleSheet.create({
    container : {
        height : 60,
        width :"100%",
        borderRadius : 20,
        borderWidth : 1,
        borderColor : "black",
        backgroundColor :"white",
        padding : 10,
        marginBottom : 10,
        justifyContent : "flex-start",
        flexDirection : "row",
        alignItems :"center"
    }

});